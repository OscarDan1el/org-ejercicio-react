import "./Header.css";

function Header (){
    /* tenemos que tener un elemento que envuelva mas elementos (padre)
    **header** */
    return <header className="header">
    <img src="/img/Header_total.png" alt='org'/>;
    </header> 
}

export default Header;